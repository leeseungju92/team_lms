<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lms.persistence.BoardDAO">
	
	<select id="boardList" resultType="BoardDTO">
		SELECT * FROM tbl_lmsboard
	</select>
	
	<insert id="boardRegister">
		INSERT INTO tbl_lmsboard(bno, name, type, title, content)
		VALUES (seq_board.NEXTVAL, '테스트이름', '테스트타입', '#{title}', '#{content}')
	</insert>
</mapper>